# 引き継ぎメモ:MiniMax H3 プロンプト設計

## いま何をやっているか

MiniMax H3(Hailuo 03)で、以下の3ショット構成の映像を出すためのプロンプトを
設計している。設計内容はすべて `MINIMAX_H3_FACE_CLOSEUP.md` にまとまっている。

```
 0.0– 3.0s  Shot 1  顔の正面を画面いっぱいに映す超クローズアップ、静止
 3.0s       ─── ハードカット ───
 3.0– 6.0s  Shot 2  全身の立ち姿、正面、3秒静止
 6.0– 7.0s  Shot 3  90°回転
 7.0– 9.0s          右側面で2秒静止
 9.0–10.0s          さらに90°回転
10.0–13.0s          背面で3秒静止
```

## 実行環境

| | |
|---|---|
| 実行場所 | ローカル PC の ComfyUI(クラウド API ではない) |
| モデル | MiniMax H3 オープンウェイト版 |
| LoRA | **Turbo LoRA(4ステップ蒸留)を使用** |
| CFG | 1.0(guidance-free) |
| 解像度 | 768 × 1344(9:16) |

**Turbo LoRA を使っている点が設計上いちばん重要。** CFG=1 なので
ネガティブプロンプト欄が無効になり、否定はすべて本文に書く必要がある。
また4ステップ蒸留はプロンプトの後半を取りこぼすので、重要な制約は先頭に置く。

## いまどこまで進んだか

- Shot 1〜3 の個別プロンプト、通しプロンプト、ComfyUI 向けの調整まで完了
- 成果物は `MINIMAX_H3_FACE_CLOSEUP.md`(1〜13章)
- **まだ実際に生成して検証はしていない**

## 次にやること

1. `MINIMAX_H3_FACE_CLOSEUP.md` の **13章「一本通し用プロンプト」** を
   ComfyUI に入れて、13秒(312フレーム @24fps)で生成する
2. シードを変えて2〜3回振る(4ステップは振れ幅が大きく、1回で判断すると誤る)
3. 下の4点をチェックする
4. ダメなら11章の4クリップ分割に切り替える

### チェックポイント

| # | 見るところ | 失敗の見え方 |
|---|---|---|
| 1 | 3秒でカットになっているか | ズームアウトで繋がっている |
| 2 | Shot 2 で頭からつま先が入っているか | 足が切れている |
| 3 | 回転がカメラの回り込みになっていないか | 背景が動いている |
| 4 | 90°×2 が完了して静止しているか | 回りきらない / 回り続ける / 往復する |

### ダメだったときの切り分け

| 症状 | 次の一手 |
|---|---|
| 3秒でカットにならない | 一本通しを諦めて11章の分割へ |
| 回転がカメラの回り込みになる | `Only the woman moves.` を GLOBAL RULES の1文目に移動 |
| 回転が完了しない・止まる | LoRA strength を 0.6〜0.75 に下げる |
| Shot 2 で足が切れる | 9:16 になっているか確認 |
| 後半だけ崩れる | 一本通しの限界。11章の分割へ |

1〜2番だけなら一本通しで粘る価値がある。3番以降が出たら分割へ。

## ファイルの構成

| ファイル | 中身 |
|---|---|
| `MINIMAX_H3_FACE_CLOSEUP.md` | 本体。1〜13章 |
| `WORKFLOW.md` | このリポジトリの使い方・引き継ぎ手順 |
| `HANDOFF.md` | このファイル |

### MINIMAX_H3_FACE_CLOSEUP.md の章立て

| 章 | 内容 |
|---|---|
| 1〜6 | Shot 1(顔の超クローズアップ)の設計と語彙 |
| 7 | Shot 2(全身の立ち姿・3秒静止) |
| 8 | Shot 3(回転して側面2秒静止 → さらに回転して3秒静止) |
| 9 | Shot 1→2→3 の全体プロンプト(クラウド API 向け) |
| 10 | ComfyUI ローカル実行の差分 |
| 11 | Turbo LoRA 向け・4クリップ分割の全文プロンプト |
| 12 | 4クリップを1回のキューで繋ぐワークフロー |
| **13** | **一本通し用プロンプト(← 次に試すのはここ)** |

## 注意点(調べ直す必要があるもの)

前セッションはネットワーク制限のある環境で作業したため、各サイトの本文が
取得できず、**検索結果のスニペットを突き合わせてまとめた部分がある**。
以下は手元の実環境で確認してほしい。

- ComfyUI のノード名(`ImageFromBatch`、`ImageBatch`、`VHS_VideoCombine` など)
- Turbo LoRA の推奨設定(shift、sampler、strength はバリアントで違う)
  - larryvrh 版と lightx2v 版のどちらを使っているかで表の該当行が変わる
- 解像度と尺の上限
