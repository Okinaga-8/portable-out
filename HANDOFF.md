# 引き継ぎ — MiniMax H3 で 1 分の動画を作る

このブランチ(`claude/minimax-h3-video-concat-p6knks`)は、**ComfyUI + MiniMax H3 をローカルで動かし、
台詞のある 1 分の動画を作りきる**ための調査ノートです。

**最終更新: 2026-09-06(ノート第 21 版)。**

**調査は終わり、実測の大半も既に埋まりました。残っているのは「連鎖とドリフト」だけです。**

> **2026-09-06 の訂正。** 第 20 版までのノートは「この PC で H3 はまだ一度も回っていない」
> という前提で書かれていました。**それは誤りでした。**同じ PC の `MiniMaxH3_work/` に
> 20 本以上の実測(OOM ゼロ)と、ノートが「最重要・未取得」と書いていた公式プロンプト
> ガイドの原文が既にありました。第 21 版で §9 と §10 を全面的に書き換えてあります。

---

## 1. まず読むもの

正本は **`docs/minimax-h3-video-concat.html`**(約 200KB)。ブラウザで開いてください。
Artifact 版もあります: https://claude.ai/code/artifact/af3d2a55-9715-4d98-af73-3b236109ffa2

**全部読む必要はありません。** 大きいので、次の順で必要な節だけ読むのが効率的です。

| 順 | 節 | 何が書いてあるか |
| --- | --- | --- |
| 1 | **§9 実験(第 21 版で全面改訂)** | **実測で埋まった前提**・**時間予算の実測表**・次にやること |
| 2 | **§10 未解決の論点(同上)** | 決着した 5 つと、**残る 4 つ** |
| 3 | **§8c ノウハウ台帳** | 9 分類・約 155 の実務ルール。**4 行を実測で訂正済み** |
| 4 | **§1b 三つの軸は独立しない** | 結合・速度・品質が互いを壊す関係。判断の土台 |
| 5 | 必要に応じて各節 | 台帳の各行に出典の節番号が振ってあります |

**確度の表記**: **`実測`(この PC で実際に測った値。いちばん強い)**/ `一次`(README や公式 PR が出典)/
`一致`(複数の二次情報が一致)/ `要検証`(単一記事、または推論)。
**`実測` は他のどの確度よりも優先してください。要検証は実機で確かめてから信じてください。**

---

## 2. 環境

| 項目 | 内容 |
| --- | --- |
| GPU | **RTX 4080 16GB** |
| システム RAM | **64GB**(H3 のエンコーダ 15.7GB + DiT 21GB が同居できる。有利な条件) |
| 実行環境 | **ComfyUI 0.33.1** / torch 2.13.0+cu130 / Python 3.13.14 / comfy-aimdo 0.4.13 / comfy-kitchen 0.2.31 |
| 起動フラグ | `--windows-standalone-build --disable-pinned-memory --use-ck-attention` |
| 量子化 | **INT8 ConvRot**(GGUF は実測で却下) |
| 目標 | **1 分**の動画 |
| 音 | **台詞・人の声が主役**(日本語) |
| 構成 | 連続シーンとカット割りが両方混ざる |
| 素材 | 生成済みクリップは無し(ゼロから) |

---

## 3. 現在地と、次にやること

**「この 4080 で何が動くか」は、もうほぼ分かっています。** 前版の 10 論点のうち 5 つが実測で決着しました。

| 決着済み | 答え |
| --- | --- |
| クリップ長 | **15 秒(362f)が通る。**0.4MP / 226.5〜247.5 秒 / **OOM なし**(16 本連続)。ただし **T2VA での値** |
| 完走できるか | **VRAM 面は心配ない。**1.0MP × 10 秒でピーク 15,238 MiB / OOM なし |
| 1 分にどれだけかかるか | **0.4MP × 15 秒なら 1 周 15〜17 分。**前版の外挿(28 分 / 2 時間 40 分)は **2〜3 倍悲観的だった** |
| 量子化 | **INT8 ConvRot 一択。GGUF は却下**(1.8〜2.6 倍遅く VRAM も増えた) |
| 32GB を借りるか | **当面いらない** |

**現行のベースライン(実測)** — モデル `10Eros_Max_h3_TURBO-hybrid_beta4_int8_convrot` /
**turbo LoRA はバイパス** / `euler` / `simple` / **6 step** / ref2va か t2va。
base に戻すなら **turbo LoRA 有効 / `res_multistep` / 8 step**。**混ぜると二重掛けになります。**

**4 step は使わないこと。**音声 Peak が −0.1〜−1.6 dB で割れます(実用域は −7 dB 前後)。
**0.4MP だと映像は一見きれいに出るので、映像だけ見て判断すると必ず間違えます。**

### 残っているのはこの 4 つ(§10 に全文)

1. **A. Ref2VA + 参照 3 枚で 15 秒が通るか** — 15 秒の実測は T2VA のもの。**1 本測れば設計が確定します**
2. **B. 何ショット目からドリフトが限界を超えるか** — **最大の未知。**この PC の実測 20 本は**すべて単発クリップ**で、連鎖は一度もやっていません
3. **C. 音は差し替えるか、リップシンクモードで妥協するか** — **セリフが終端 0.2 秒前まで来る**と分かったので、差し替え方式が有利になりました
4. **D. 画像列で渡すか、潜在で渡すか** — B の結果で決まります

**測定シート(所要時間 / 継ぎ目 / 顔と手の破綻)を毎回記録し、1 回に 1 つだけ変えること。**
**このパイプラインは完全に決定的です**(同 seed・同設定で SSIM Y = 1.000000)。
**だから 1 つだけ変えた比較には、その差以外のノイズが乗りません。**

---

## 4. 決まっていること(要点だけ)

- **音は差し替える。** 日本語 TTS で台詞を作り、H3 にはリップシンクさせ、**最終出力の音声トラックを TTS 音声に置き換える**。
  出力音声は常に合成されるので(公式仕様)、これが**日本語のアクセント問題への唯一確実な答え**です
- **参照は 1 枚のシートに集約する。** 枚数を増やすほど品質が落ちます。ただし長辺は短辺の 2.5 倍まで
- **クリップ長は `length % 17 == 5`(24fps)** — 5, 22, 39, 56…
- **セリフはクリップの終端 0.20〜0.24 秒前に張り付く**(実測 3 本)。**ショットを伸ばして
  増えるのは発話「前」の無音で、セリフ側の余裕ではありません。**継ぎ目のクロスフェードは
  語尾を削る危険があります(音を差し替えるなら問題ごと消えます)
- **VRAM ピークは OOM 接近の指標にならない。**Dynamic VRAM が活性化領域に合わせて常駐重みを
  降ろすので、**解像度と尺を上げるとサンプリング中の VRAM はむしろ減ります**
- **速度改善は品質改善。** 2/3 のテイクを捨てるのが普通なので、速度が 3 倍になれば作り込みも 3 倍できます
- **カット割りは技術的な劣化リセット。** 連続でなくてよい継ぎ目は、迷わず切る
- **プロンプトは 5 層**(被写体定義 → 要約 → retention → 詳細記述 → 制約と音声)。
  発話は `<d>[Japanese]…</d>` でくくる(くくらないとト書きを読み上げます)
- **解像度を上げるのは最後。** 低解像度で構成を固めてから

---

## 5. 保留(条件が来たら再浮上する)

- **PDD 加速 LoRA** — 前版の本命でしたが、**売りだった「20 step → 8 step」は既に 6 step で
  回っているので価値が薄れました。**残る利点は Ref2VA のキャラ一貫性で、**論点 B の結果が
  よくなければ再浮上します。**試すなら **base に戻してから**(beta4 は turbo 焼き込み済みで、
  重ねると二重蒸留になります)
- **PyTorch の入替** — **優先度は最下位。**根拠だった「入替だけで 2.17 倍」は古い同梱 torch が
  前提で、この PC は既に `torch 2.13.0+cu130`。**壊すと全部止まるので実験 4・5 の後に**
- **Chunk FeedForward / W4A8 / 32GB レンタル** — **OOM したときだけ。**いまは不要
- **EasyCache / conditioning キャッシュ** — **触らない。**EasyCache が主に壊すのは音声で、
  台詞が主役の本件と相性が悪い。速度で困っていないうちは変数を増やさないほうが得です

**公式プロンプト指針は取得済みでした。**ダウンロード不要です:
`MiniMaxH3_work/docs_archive/official_prompt_guides/VIDEO_PROMPT_WRITING_GUIDE_ref_en.md`
(23.0KB / **Ref2VA 用。本件の本命**)と `_base_en.md`(15.4KB)。**最初にこれを読んでください。**

**まだ未取得なのは**「H3 long form blueprint」(動画の説明欄からダウンロード)だけです。

---

## 5b. 未処理(2026-09-06 時点)

- **Artifact `af3d2a55-9715-4d98-af73-3b236109ffa2` はまだ第 20 版のまま。**
  再公開には「ライブ版を全行読む」手順が要り、322KB あるので次のセッションに送った。
  **ライブ版は第 20 版とバイト一致を確認済み**(差分は host が付ける doctype ラッパーのみ)
  なので、**マージすべき内容は無い。読んだら第 21 版をそのまま上書きしてよい。**
  Artifact 用の本体は `docs/minimax-h3-video-concat.html` から
  doctype / html / head / body を外し、`<div class="wrap">` の直前に
  `<!--HEADEND-->` を挿すだけで作れる
- **`MiniMaxH3_work/` 側の `portable-out` 作業ツリーは、いま
  `claude/minimax-h3-video-concat-p6knks` を checkout している。**
  `claude/minimax-h3-face-prompt-lpoapj` の未コミット分(14章・実機照合)は
  **`git stash` に退避してある**(`stash@{0}`)。face-prompt に戻るときは
  checkout してから `git stash pop`
- **新しい情報源(YouTube / note)を追加する話が出ている。**
  **字幕全文・本文テキストを貼ってもらうのが最短**(前回 §11 が推測から要約に変わったのは
  それが理由)。`youtube.com` / `note.com` は cloud 側ではブロックされていた。
  **ローカルから通るかは未確認**

## 6. このリポジトリの流儀

- **ノートは新規ファイルを作らず、`docs/minimax-h3-video-concat.html` を書き換えて更新する**
- 更新したら Artifact も同じ URL に再公開する(URL を変えない)
- `.claude/skills/research-note/` に、この形式のノートを作る手順が入っています
- 経緯はすべて git のコミットログに残っています。`git log --oneline` で追えます
