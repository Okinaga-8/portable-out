# 引き継ぎ — MiniMax H3 で 1 分の動画を作る

このブランチ(`claude/minimax-h3-video-concat-p6knks`)は、**ComfyUI + MiniMax H3 をローカルで動かし、
台詞のある 1 分の動画を作りきる**ための調査ノートです。

**最終更新: 2026-09-06(ノート第 23 版)。**

**調査は終わり、実測の大半も既に埋まりました。残っているのは「連鎖とドリフト」だけです。**

> **2026-09-06 の訂正。** 第 20 版までのノートは「この PC で H3 はまだ一度も回っていない」
> という前提で書かれていました。**それは誤りでした。**同じ PC の `MiniMaxH3_work/` に
> 20 本以上の実測(OOM ゼロ)と、ノートが「最重要・未取得」と書いていた公式プロンプト
> ガイドの原文が既にありました。第 21 版で §9 と §10 を全面的に書き換えてあります。

---

## 1. まず読むもの

正本は **`docs/minimax-h3-video-concat.html`**(約 412KB)。ブラウザで開いてください。
Artifact 版もあります: https://claude.ai/code/artifact/af3d2a55-9715-4d98-af73-3b236109ffa2

> **⚠ 正本を丸ごと読ませないこと。412KB あります。**
> **入口はこの `HANDOFF.md`(12KB)で合っています。**下の表の順で、必要な節だけ open してください。
> **人間が読むだけなら Artifact 版が速い**（スマホでも開けます）。
>
> **クラウド（スマホ）から再開するときの注意:**
> - **見えるのは push 済みのものだけ。**ローカルのコミットも `git stash` も向こうには存在しません
> - **取得できるものが減ります** — GitHub は通りますが
>   `note.com` / `youtube.com` / `huggingface.co` はブロックされます
>   （ローカルなら `note.com` と GitHub は通る。YouTube はどちらでも中身が取れない）
> - **新しい情報源は、字幕全文・本文テキストを貼ってもらうのが確実です**

**全部読む必要はありません。** 大きいので、次の順で必要な節だけ読むのが効率的です。

| 順 | 節 | 何が書いてあるか |
| --- | --- | --- |
| 1 | **§9 実験(第 21 版で全面改訂)** | **実測で埋まった前提**・**時間予算の実測表**・次にやること |
| 2 | **§10 未解決の論点(同上)** | 決着した 5 つと、**残る 4 つ** |
| 3 | **§8c ノウハウ台帳** | 9 分類・**177** の実務ルール。**4 行を実測で訂正済み**、第 22 版 +20 行 / 第 23 版 +16 行 |
| 4 | **§1b 三つの軸は独立しない** | 結合・速度・品質が互いを壊す関係。判断の土台 |
| 5 | 必要に応じて各節 | 台帳の各行に出典の節番号が振ってあります |

**確度の表記**: **`実測`(この PC で実際に測った値。いちばん強い)**/ `一次`(README や公式 PR が出典)/
`一致`(複数の二次情報が一致)/ `要検証`(単一記事、または推論)。
**`実測` は他のどの確度よりも優先してください。要検証は実機で確かめてから信じてください。**

---

## 2. 環境

| 項目 | 内容 |
| --- | --- |
| GPU | **RTX 4080 16GB** |
| システム RAM | **64GB**(H3 のエンコーダ 15.7GB + DiT 21GB が同居できる。有利な条件) |
| 実行環境 | **ComfyUI 0.34.5**（2026-09-06 に 0.33.1 から更新）/ torch 2.13.0+cu130 / Python 3.13.14 / **comfy-aimdo 0.4.15** / comfy-kitchen 0.2.31 |
| 更新の記録 | `update\update_comfyui_stable.bat` 相当（`update.py --stable`）で実施。**torch / torchvision / torchaudio / comfy-kitchen は無傷**、comfy-aimdo だけ 0.4.13 → 0.4.15 に上がった。**依存を上げる `update_comfyui_and_python_dependencies.bat` は使っていない**（あれは torch を差し替える） |
| 退避先 | 壊れたら `git -C "…\ComfyUI_windows_portable\ComfyUI" checkout v0.33.1`（更新前は commit `72865f4`）。更新時に `backup_branch_2026-09-06_16_50_21` も自動作成されている |
| 起動フラグ | `--windows-standalone-build --disable-pinned-memory --use-ck-attention` |
| 量子化 | **INT8 ConvRot**(GGUF は実測で却下) |
| 目標 | **1 分**の動画 |
| 音 | **台詞・人の声が主役**(日本語) |
| 構成 | 連続シーンとカット割りが両方混ざる |
| 素材 | 生成済みクリップは無し(ゼロから) |

---

## 3. 現在地と、次にやること

**「この 4080 で何が動くか」は、もうほぼ分かっています。** 前版の 10 論点のうち 5 つが実測で決着しました。

| 決着済み | 答え |
| --- | --- |
| クリップ長 | **15 秒(362f)が通る。**0.4MP / 226.5〜247.5 秒 / **OOM なし**(16 本連続)。ただし **T2VA での値** |
| 完走できるか | **VRAM 面は心配ない。**1.0MP × 10 秒でピーク 15,238 MiB / OOM なし |
| 1 分にどれだけかかるか | **0.4MP × 15 秒なら 1 周 15〜17 分。**前版の外挿(28 分 / 2 時間 40 分)は **2〜3 倍悲観的だった** |
| 量子化 | **INT8 ConvRot 一択。GGUF は却下**(1.8〜2.6 倍遅く VRAM も増えた) |
| 32GB を借りるか | **当面いらない** |

**現行のベースライン(実測)** — モデル `10Eros_Max_h3_TURBO-hybrid_beta4_int8_convrot` /
**turbo LoRA はバイパス** / `euler` / `simple` / **6 step** / ref2va か t2va。
base に戻すなら **turbo LoRA 有効 / `res_multistep` / 8 step**。**混ぜると二重掛けになります。**

**4 step は使わないこと。**音声 Peak が −0.1〜−1.6 dB で割れます(実用域は −7 dB 前後)。
**0.4MP だと映像は一見きれいに出るので、映像だけ見て判断すると必ず間違えます。**

### 残っているのはこの 4 つ(§10 に全文)

1. **A. Ref2VA + 参照 3 枚で 15 秒が通るか** — 15 秒の実測は T2VA のもの。**1 本測れば設計が確定します**
2. **B. 何ショット目からドリフトが限界を超えるか** — **最大の未知。**この PC の実測 20 本は**すべて単発クリップ**で、連鎖は一度もやっていません。
   **第 23 版で初めて外からの数字が付きました**(§11k)— 同じ「しゃべり続ける」構成で
   **4 クリップは継ぎ目が分からず、6 クリップで顔に段階的な劣化**。
   原因は「**各セグメントは 1 つ前しか見ない。元へ引き戻すものが無い**」。
   → **キャラシートを毎回渡すのは、その引き戻す力を外から足す手当てだった。**
   **ただし高解像度のほうが一貫性も良かったという報告があるので、
   ドリフトの限界を低解像度で測ると悲観的に出る可能性がある**
3. **C. 音は差し替えるか、リップシンクモードで妥協するか** — **セリフが終端 0.2 秒前まで来る**と分かったので、差し替え方式が有利になりました
4. **D. 画像列で渡すか、潜在で渡すか** → **第 23 版で問いが組み替わりました。**
   **潜在ハンドオーバーと VAE ハンドオーバーを比べて「品質に大差なし」**という実測報告(§11k)。
   §11j の「潜在保持だと継ぎ目にグリッチが出うる」と同じ向きで、**潜在に賭ける根拠は弱い。**
   代わりに**選択肢そのものが増えました** — **標準ノード `MiniMaxH3AddGuide` で
   「終端フレーム群 + 音声」を渡すオーバーラップ連鎖**(§4 の B')。**カスタムノード不要。**
   **→ D は「画像列か潜在か」ではなく「まず B' を試す」に読み替える。**

**~~実験 4 の前に ComfyUI を 0.34.0 以降へ~~ → 2026-09-06 に 0.34.5 へ更新して解消。**
**連鎖に入れる状態になりました。**実機で確認済み(この PC での `実測`):

- `MiniMaxH3AddGuide` / `MiniMaxH3ReferenceToVideo` / `SamplerCustomAdvanced` は**すべて存在する**
- **`MiniMaxH3AddGuide` の入力は `latent` / `positive` / `vae` / `audio_vae` / `image` / `audio` / `frame_idx`。**
  **`frame_idx` があり、`image` と `audio` を同じノードで受ける** — §11k の
  「attention にフレームインデックスを指定して、終端フレーム群 + 音声を渡す」が
  **ノードの入力そのもので裏付けられました**
- 起動ログに **`Using Comfy Kitchen attention`** が出る（CK INT8 は生きている）。
  comfy-aimdo 0.4.15 も RTX 4080 を掴んでいる。**エラー・警告ゼロ**
- **ただし実際に生成は回していません。**次にやるのは
  **同 seed・同設定で 1 本焼いて、更新前と一致するかの確認**です
  （このパイプラインは決定的なので、ずれたら更新が原因と断定できます）

そして**測定ツールを自分で書く前に `loopforge0/ComfyUI-H3-Continuous` の `tools/` を見ること**
(`face_drift.py` / `latent_drift.py` / `bench_chain.py` が同梱されています)。

**ついでに片付く安い比較が 1 つあります(§11j)。**
**scheduler が `simple` か `beta` かは未決着**です。この PC の実測は `euler`+`simple`、
別の実務者は `euler`+`beta`。**1 変数だけ変えて 1 本ずつ回せば終わります。**
実験 3 のついでに入れてください。

**測定シート(所要時間 / 継ぎ目 / 顔と手の破綻)を毎回記録し、1 回に 1 つだけ変えること。**
**このパイプラインは完全に決定的です**(同 seed・同設定で SSIM Y = 1.000000)。
**だから 1 つだけ変えた比較には、その差以外のノイズが乗りません。**

---

## 4. 決まっていること(要点だけ)

- **音は差し替える。** 日本語 TTS で台詞を作り、H3 にはリップシンクさせ、**最終出力の音声トラックを TTS 音声に置き換える**。
  出力音声は常に合成されるので(公式仕様)、これが**日本語のアクセント問題への唯一確実な答え**です
- **参照は 1 枚のシートに集約する。** 枚数を増やすほど品質が落ちます。ただし長辺は短辺の 2.5 倍まで
- **クリップ長は `length % 17 == 5`(24fps)** — 5, 22, 39, 56…
- **セリフはクリップの終端 0.20〜0.24 秒前に張り付く**(実測 3 本)。**ショットを伸ばして
  増えるのは発話「前」の無音で、セリフ側の余裕ではありません。**継ぎ目のクロスフェードは
  語尾を削る危険があります(音を差し替えるなら問題ごと消えます)
- **VRAM ピークは OOM 接近の指標にならない。**Dynamic VRAM が活性化領域に合わせて常駐重みを
  降ろすので、**解像度と尺を上げるとサンプリング中の VRAM はむしろ減ります**
- **速度改善は品質改善。** 2/3 のテイクを捨てるのが普通なので、速度が 3 倍になれば作り込みも 3 倍できます
- **カット割りは技術的な劣化リセット。** 連続でなくてよい継ぎ目は、迷わず切る
- **プロンプトは 5 層**(被写体定義 → 要約 → retention → 詳細記述 → 制約と音声)。
  発話は `<d>[Japanese]…</d>` でくくる(くくらないとト書きを読み上げます)
- **解像度を上げるのは最後。** 低解像度で構成を固めてから

---

## 5. 保留(条件が来たら再浮上する)

- **PDD 加速 LoRA** — 前版の本命でしたが、**売りだった「20 step → 8 step」は既に 6 step で
  回っているので価値が薄れました。**残る利点は Ref2VA のキャラ一貫性で、**論点 B の結果が
  よくなければ再浮上します。**試すなら **base に戻してから**(beta4 は turbo 焼き込み済みで、
  重ねると二重蒸留になります)
- **PyTorch の入替** — **優先度は最下位。**根拠だった「入替だけで 2.17 倍」は古い同梱 torch が
  前提で、この PC は既に `torch 2.13.0+cu130`。**壊すと全部止まるので実験 4・5 の後に**
- **Chunk FeedForward / W4A8 / 32GB レンタル** — **OOM したときだけ。**いまは不要
- **EasyCache / conditioning キャッシュ** — **触らない。**EasyCache が主に壊すのは音声で、
  台詞が主役の本件と相性が悪い。速度で困っていないうちは変数を増やさないほうが得です

**公式プロンプト指針は取得済みでした。**ダウンロード不要です:
`MiniMaxH3_work/docs_archive/official_prompt_guides/VIDEO_PROMPT_WRITING_GUIDE_ref_en.md`
(23.0KB / **Ref2VA 用。本件の本命**)と `_base_en.md`(15.4KB)。**最初にこれを読んでください。**

**まだ未取得なのは**「H3 long form blueprint」(動画の説明欄からダウンロード)だけです。

---

## 5b. 未処理(2026-09-06 時点)

- **~~Artifact が第 20 版のまま~~ → 2026-09-06 に第 21 版を再公開して解消。**
  URL は変わっていない(`af3d2a55-9715-4d98-af73-3b236109ffa2`)。
  上書き前にライブ版を全行読んで照合したところ、**321,656 バイトすべてが第 20 版と一致**
  (差は末尾の空行 1 バイトのみ)。**外部からの編集は無かった。**
  変換規則が確定した — `docs/minimax-h3-video-concat.html` から
  **doctype / html / head / body の各行と `<meta charset>` / `<meta name="viewport">` の
  2 行を落とし**、`<div class="wrap">` の直前に `<!--HEADEND-->` を挿す。
  そして**改行を LF に正規化する**。作業ツリーは `core.autocrlf=true` で CRLF なので、
  **そのまま出すと全 3,600 行が差分になる**。(`git show HEAD:docs/...` を変換元にしても
  LF になるが、**未コミットの改訂は入らない**ので、変換側で正規化するほうが安全)
- **`MiniMaxH3_work/` 側の `portable-out` 作業ツリーは、いま
  `claude/minimax-h3-video-concat-p6knks` を checkout している。**
  `claude/minimax-h3-face-prompt-lpoapj` の未コミット分(14章・実機照合)は
  **`git stash` に退避してある**(`stash@{0}`)。face-prompt に戻るときは
  checkout してから `git stash pop`
- **~~新しい情報源の追加~~ → 2026-09-06、動画 10 を §11j に取り込んで第 22 版として再公開済み。**
  MuseCollective の Director V1.2(`youtu.be/41R-wFwEVIM`)。**ノード固有の機能と H3 自体の挙動を
  切り分けて収録**し、後者を台帳 §8c に 20 行追記した。とくに効くのは
  **リップシンクは音声を渡すだけでは同期しない**(台詞をタイムスタンプ位置に置く必要がある。
  **日本語 TTS 方針なら書き起こしが要らない**)、**参照 9 枠は Location と継続アンカーに食われて
  実質 7 枠・超えても黙って落ちる**、**二段サンプリング 1 段目の音は判断材料にならない**の 3 つ。
  **リポジトリの README は動画より新しく、参照枠の数字を上書きしていた**(9→8 ではなく 9→7)。
- **動画 11 を §11k に追加(第 23 版)。** Loop Forge の
  「longer videos without visible joins」(`youtu.be/kj6tNy6NXH4`)+
  `github.com/loopforge0/ComfyUI-H3-Continuous`。**残論点 B と D に初めて外からの材料が付いた。**
  **最大の発見は「連鎖の中核が標準ノードで組める」こと** — H3 は attention にフレームインデックスを
  指定でき、**`MiniMaxH3AddGuide` が終端フレーム群 + 音声を渡してオーバーラップさせる**。
  §4 に選択肢 **B'** を追記した。**ハンドオフは 5/22/39/56 フレーム(既定 39f・推奨はショット長の 1/5)で、
  §11 の「画像列は 3 秒以上」とは別物。**環境は **RTX 3060 12GB** で、この PC より下。
- **情報源の渡し方 — 2026-09-06、ローカルからの到達性を実測した**(cloud 側では両方ブロックされていた):
  - **`note.com` は通る。** 本文がそのまま取れたので、**URL を貼るだけでよい**
  - **`youtube.com` は通るが中身が取れない。** ページには到達するがフッターしか返らず、
    タイトル・チャンネル名・説明欄・字幕のいずれも取得できない(JS 描画のため)。
    **YouTube は従来どおり字幕全文を貼ってもらう**(前回 §11 が推測から要約に
    変わったのはそれが理由)
  - GitHub は従来どおり通る

## 6. このリポジトリの流儀

- **ノートは新規ファイルを作らず、`docs/minimax-h3-video-concat.html` を書き換えて更新する**
- 更新したら Artifact も同じ URL に再公開する(URL を変えない)
- `.claude/skills/research-note/` に、この形式のノートを作る手順が入っています
- 経緯はすべて git のコミットログに残っています。`git log --oneline` で追えます
